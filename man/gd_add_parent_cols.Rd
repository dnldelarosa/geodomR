% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{gd_add_parent_cols}
\alias{gd_add_parent_cols}
\title{Agregar columnas de niveles superiores a un data frame}
\usage{
gd_add_parent_cols(
  data,
  .levels = NULL,
  .level = NULL,
  .name = NULL,
  .key = NULL,
  .clean = TRUE,
  .tol = 0.25,
  .on_error = "na"
)
}
\arguments{
\item{data}{Un data frame con una columna que identifica un nivel administrativo.}

\item{.levels}{Vector de caracteres con los niveles superiores que se desean agregar.
Opciones: \code{"regions"}, \code{"provinces"}, \code{"municipalities"}, \code{"dm"}, \code{"sections"}.
Si es \code{NULL} (por defecto), se agregan todos los niveles superiores disponibles.}

\item{.level}{Cadena de caracteres opcional especificando el nivel administrativo
del data frame. Si no se proporciona, se detecta automáticamente.}

\item{.name}{Cadena de caracteres opcional especificando el nombre de la variable
en \code{data} que contiene los nombres de las unidades administrativas.}

\item{.key}{Cadena de caracteres opcional especificando la clave del nivel.}

\item{.clean}{Lógico. Si \code{TRUE} (por defecto), limpia y estandariza los nombres
geográficos en la columna detectada del data frame.}

\item{.tol}{Tolerancia para la limpieza de nombres (Jaro-Winkler). Por defecto 0.25.}

\item{.on_error}{Manejo de errores en la limpieza: \code{"fail"}, \code{"na"} o \code{"omit"}.
Por defecto \code{"na"} para evitar que un solo nombre inválido detenga el proceso.}
}
\value{
Un data frame con columnas adicionales para cada nivel superior solicitado.
Las columnas nuevas se nombran en español: Region, Provincia, Municipio,
Distrito_Municipal, Seccion.
}
\description{
Esta función enriquece un data frame que contiene datos a un nivel
administrativo determinado (ej: provincias, municipios) con columnas
de nombre para los niveles superiores en la jerarquía territorial.
}
\details{
La jerarquía administrativa de la República Dominicana es:
Regiones > Provincias > Municipios > Distritos Municipales > Secciones > Barrios/Parajes
}
\examples{
\dontrun{
  # Agregar región a datos con columna de provincia
  datos <- data.frame(
    provincia = c("Santo Domingo", "Santiago", "La Vega", "Puerto Plata"),
    poblacion = c(2500000, 1000000, 400000, 320000)
  )
  datos_con_region <- gd_add_parent_cols(datos)

  # Agregar sólo regiones (no todos los niveles superiores)
  datos_con_region <- gd_add_parent_cols(datos, .levels = "regions")

  # Agregar provincia y región a datos de municipios
  datos_mun <- data.frame(
    municipio = c("Santo Domingo Este", "Santiago", "Moca"),
    valor = 1:3
  )
  datos_enriquecidos <- gd_add_parent_cols(datos_mun)

  # Sin limpiar la columna original
  datos_enriquecidos <- gd_add_parent_cols(datos, .clean = FALSE)
}
}
